apiVersion: v1
kind: Service
metadata:
  name: {{ include "drinkin-water.fullname" . }}
  labels:
    {{- include "drinkin-water.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drinkin-water.selectorLabels" . | nindent 4 }}
