{{- if .Values.cde-app.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "defra-cde.fullname" . }}-app
  annotations:
    kubernetes.io/ingress.class: nginx    
    cert-manager.io/cluster-issuer: letsencrypt-prod
    ingress.kubernetes.io/proxy-body-size: '0'
    nginx.ingress.kubernetes.io/whitelist-source-range: 0.0.0.0/0
  labels:
    app: {{ include "defra-cde.fullname" . }}-app
spec:
  rules:
  {{- range .Values.cde-app.ingress.hosts }}
  - host: {{ .host }}
    http:
      paths:
      - backend:
          service: 
            name: {{ include "defra-cde.fullname" $ }}-app
            port: 
              number: 80
        path: /catchment-planning/
        pathType: Prefix
  {{- end }}
  {{- if .Values.cde-app.ingress.tls.enabled }}
  tls:
    - hosts:
        {{- range .Values.cde-app.ingress.hosts }}
        - {{ .host }}
        {{- end }}
      secretName: {{ include "defra-cde.fullname" . }}-app-tls
  {{- end }}
{{- end }}
