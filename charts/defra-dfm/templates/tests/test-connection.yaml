apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "defra-dfm.fullname" . }}-test-connection"
  labels:
    {{- include "defra-dfm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "defra-dfm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
